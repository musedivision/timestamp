function checkType(str) {
    // it can be unix - numbers only about 12
    // natural - numbers and string - January 1st, 2016
        // mixed up order!
}

function timestamp(param) {
    var query = param["0"].slice(1)
    console.log(query)
    return query
}

module.exports = timestamp